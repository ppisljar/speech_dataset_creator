# validate speakers 

# validate text files against audio files ?

# outlier detection ?

def validate_transcription(transcription_json, audio_file):
    """
    Validate the transcription JSON against the audio file.
    
    Args:
        transcription_json (str): Path to the transcription JSON file.
        audio_file (str): Path to the audio file.
    
    Returns:
        bool: True if validation passes, False otherwise.
    """
   
    return True
